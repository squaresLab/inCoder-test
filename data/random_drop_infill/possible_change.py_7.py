{"text": "def possible_change(coins, total):\n    if total == 0:\n        return 1\n    if total < 0:\n        return 0\n\n    coins = sorted(coins)\n    first = coins[0]\n    rest = coins[1:]\n    return possible_change(coins, total - first) + possible_change(rest, total)", "parts": ["def possible_change(coins, total):\n    if total == 0:\n        return 1\n    if total < 0:\n        return 0\n\n    ", "\n    return possible_change(coins, total - first) + possible_change(rest, total)"], "infills": ["coins = sorted(coins)\n    first = coins[0]\n    rest = coins[1:]"], "retries_attempted": 1}