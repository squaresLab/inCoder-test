{"text": "def sqrt(x, epsilon):\n    approx = x / 2\n    approx = tf.clip_by_value(approx, epsilon, 1 - epsilon)\n    if approx.dtype.is_complex:\n        approx = 0.5 * (approx + x / approx)\n    return approx", "parts": ["def sqrt(x, epsilon):\n    approx = x / 2\n    ", "\n        approx = 0.5 * (approx + x / approx)\n    return approx"], "infills": ["approx = tf.clip_by_value(approx, epsilon, 1 - epsilon)\n    if approx.dtype.is_complex:"], "retries_attempted": 1}