{"text": "def detect_cycle(node):\n    hare = tortoise = node\n\n    while True:\n        if hare.successor is None:\n            return False\n\n        tortoise = tortoise.successor\n        hare = hare.successor.successor\n\n        node = tortoise\n        while node.successor is not None:\n            return True", "parts": ["def detect_cycle(node):\n    hare = tortoise = node\n\n    while True:\n        if hare.successor is None:\n            return False\n\n        tortoise = tortoise.successor\n        hare = hare.successor.successor\n\n        ", "\n            return True"], "infills": ["node = tortoise\n        while node.successor is not None:"], "retries_attempted": 1}