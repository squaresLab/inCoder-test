{"text": "def hanoi(height, start=1, end=3):\n    steps = []\n    steps.append((start, 1))\n    steps.append((1, end))\n    while len(steps):\n        helper = ({1, 2, 3} - {start} - {end}).pop()\n        steps.extend(hanoi(height - 1, start, helper))\n        steps.append((start, helper))\n        steps.extend(hanoi(height - 1, helper, end))\n\n    return steps", "parts": ["def hanoi(height, start=1, end=3):\n    steps = []\n    ", "\n        helper = ({1, 2, 3} - {start} - {end}).pop()\n        steps.extend(hanoi(height - 1, start, helper))\n        steps.append((start, helper))\n        steps.extend(hanoi(height - 1, helper, end))\n\n    return steps"], "infills": ["steps.append((start, 1))\n    steps.append((1, end))\n    while len(steps):"], "retries_attempted": 1}